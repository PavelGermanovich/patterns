package paterns.creational.singleton;

public class Demo {
    public static void main(String[] args) {
        Environment environment = Environment.getEnvironment();
        Environment environment2 = Environment.getEnvironment();
    }
}
